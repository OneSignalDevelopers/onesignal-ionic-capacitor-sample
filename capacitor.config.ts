import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic and Flutter',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
